/*
#include "Compression.hpp"
#include "GtestWrapper.hpp"
#include "RangeStlAlgorithms.hpp"

TEST(Compression, isSymmetric)
{
    IOBuffer buffer;
    iota(buffer, 0);
    EXPECT_EQ(buffer, decompress(compress(buffer)));
}
*/
